linters:
  enable:
    - gofmt
    - goimports
    - govet
    - staticcheck
    - unused
    - typecheck
    - ineffassign

linters-settings:
  goimports:
    local-prefixes: github.com/lucasew/imgcode

run:
  timeout: 5m
